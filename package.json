{
  "name": "aituber-radio",
  "version": "0.8.0",
  "description": "Chrome Extension for AITuber radio broadcasting with VRM + VOICEVOX + LLM",
  "scripts": {
    "index": "node generate-index.js",
    "build": "node generate-index.js && esbuild viewer/viewer.js --bundle --format=esm --outfile=viewer/viewer.bundle.js --minify",
    "watch": "esbuild viewer/viewer.js --bundle --format=esm --outfile=viewer/viewer.bundle.js --watch"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "type": "commonjs",
  "dependencies": {
    "@pixiv/three-vrm": "^3.5.1",
    "esbuild": "^0.27.4",
    "three": "^0.183.2"
  }
}